//import styles from "../styles/Header.module.css";
import {Link} from "react-router";

const Header = () => {
    return (
        <div>
            hello this is the profile
        </div>
    );
};

export default Header;